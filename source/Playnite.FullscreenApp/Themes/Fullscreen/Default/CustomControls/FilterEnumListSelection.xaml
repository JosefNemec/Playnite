<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrl="clr-namespace:Playnite.FullscreenApp.Controls;assembly=Playnite.FullscreenApp"
                    >

    <Style TargetType="{x:Type ctrl:FilterEnumListSelection}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctrl:FilterEnumListSelection}">
                    <DockPanel Width="{DynamicResource FilterPanelWidth}"
                               Background="{DynamicResource OverlayMenuBackgroundBrush}"
                               x:Name="PART_MenuHost">
                        <ctrl:ButtonEx DockPanel.Dock="Top" Margin="15,5,15,5" BorderThickness="2"                                                             
                                  x:Name="PART_ButtonBack">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xeab5;" FontFamily="{StaticResource FontIcoFont}"
                                               FontSize="20" VerticalAlignment="Center"
                                               Margin="0,0,10,0" />
                                    <TextBlock Text="{Binding Title}"
                                               VerticalAlignment="Center"
                                               Style="{DynamicResource TextBlockBaseStyle}"/>
                                </StackPanel>
                            </Button.Content>
                        </ctrl:ButtonEx>

                        <ctrl:ButtonEx Content="{DynamicResource LOCClearLabel}" DockPanel.Dock="Top"
                                  Margin="15,5,15,5" BorderThickness="2"
                                  x:Name="PART_ButtonClear"/>

                        <ctrl:ItemsControlEx x:Name="PART_ItemsHost"
                                        DockPanel.Dock="Top" Focusable="False"
                                        KeyboardNavigation.DirectionalNavigation="Once" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>