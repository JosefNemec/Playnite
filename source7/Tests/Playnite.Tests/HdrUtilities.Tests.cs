namespace Playnite.Tests;

[TestFixture]
public class HdrUtilitiesTests
{
    [Test]
    public void MethodsDontThrowTest()
    {
        //Assert.IsTrue(AppConfig.ThrowAllErrors);
        //Assert.DoesNotThrow(() => HdrUtilities.IsHdrEnabled());
        //Assert.DoesNotThrow(() => HdrUtilities.IsHdrSupported());
    }
}
